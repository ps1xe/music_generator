import { Effect } from "redux-saga/effects.js";

function* Saga(action: object): Generator<Effect, void> {
//   try {
//     const page = action.payload;
//     return
//   } catch (exception) {
//     return exception;
//   }
}
